<h1>🚀 New Letters 🚀</h1>

<div class="container">
  <ul class="list">
    <% @letters.each do |letter| %>
      <li><%= letter %></li>
    <% end %>
  </ul>
</div>

<div class="container">
  <div class="guess">
    <h3>What is the longest word you can find?</h3>
    <form action="/score" method="post">
      <input type="text" name="word" value=<%= params[:word] %>>
      <input type="submit" type="Ask!">
      <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
    </form>
  </div>
</div>
